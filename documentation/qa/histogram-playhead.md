# Histogram playhead (iPhone 390×844)

Session detail Debug → Volume Histogram on a simulated 8s recording (`?simulate=1&histogram=1`).

- `histogram-playhead-idle-iphone.png` — playhead hidden before first play (`0:00 / 0:08`). Amber snip `1` at t=0.
- `histogram-playhead-playing-iphone.png` — solid white playhead at ~0:03 while playing. `data-playhead≈3.39`, `data-duration≈8.96`.
- `histogram-playhead-seeked-iphone.png` — playhead stays after pause/seek (~0:05). Snip marker still at the left edge.
